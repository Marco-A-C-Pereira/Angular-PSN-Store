import { ReplacerArrPipe } from './replacer-arr.pipe';

describe('ReplacerArrPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplacerArrPipe();
    expect(pipe).toBeTruthy();
  });
});
